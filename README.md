# 🕵️‍♂️ Educational Plagiarism Detector  
**Интеллектуальная система поиска заимствований в учебных работах**

Этот проект разработан в рамках финального задания курса.  
Программа анализирует текстовые файлы, вычисляет степень их сходства с помощью методов машинного обучения (NLP) и строит визуальную карту плагиата.

---

## 📌 Описание проекта

Проблема проверки учебных работ на уникальность актуальна для любого учебного заведения.  
Ручная сверка десятков файлов — долгий и трудозатратный процесс.  

Данный проект автоматизирует эту задачу, используя математический подход:

1. **Векторизация (TF-IDF):** Тексты преобразуются в числовые векторы с учётом важности редких слов.  
2. **Косинусное сходство (Cosine Similarity):** Вычисляется угол между векторами документов. Чем меньше угол — тем выше степень сходства.

---

### ✨ Функциональные возможности

- **Сравнение "все со всеми":** Строит матрицу схожести для любого количества файлов.  
- **Визуализация:** Генерирует наглядную тепловую карту (Heatmap), где плагиат подсвечен красным.  
- **Машиночитаемый отчёт:** Сохраняет результаты в формате JSON.  
- **Автоматизация (CI/CD):** Проверка выполняется автоматически при загрузке работ в репозиторий.

---

## 📂 Структура проекта

plagiarism_project/
├── .github/workflows/ # Настройки CI/CD (GitHub Actions)
├── src/ # Исходный код
│ ├── main.py # Точка входа (запуск программы)
│ └── plagiarism.py # Класс детектора (логика ML)
├── tests/ # Автотесты
├── uploads/ # Папка для загрузки работ студентов (.txt)
├── reports/ # Папка для сохранения результатов (графики, JSON)
├── requirements.txt # Список библиотек
└── README.md # Документация

text

---

## 🛠 Технологический стек

- **Python 3.10+**  
- **Scikit-learn:** реализация TF-IDF и Cosine Similarity  
- **NumPy:** работа с матрицами  
- **Seaborn & Matplotlib:** визуализация данных (Heatmap)  
- **Pytest:** модульное тестирование  

---

## 🚀 Инструкция по запуску

### 1. Установка окружения

Убедитесь, что установлен Python и выполните установку зависимостей:

pip install -r requirements.txt

text

### 2. Подготовка данных

Поместите текстовые файлы, которые нужно проверить, в папку `uploads/`.  
Файлы должны иметь расширение `.txt`.

Пример:

uploads/ivanov.txt
uploads/petrov.txt
uploads/wikipedia_copy.txt

text

### 3. Запуск анализатора

Выполните команду:

python src/main.py

text

### 4. Просмотр результатов

После успешного выполнения программы откройте папку `reports/`:

- 📊 `similarity_matrix.png` — тепловая карта плагиата  
- 📝 `results.json` — точные проценты совпадений

---

## 🧪 Тестирование

В проекте реализованы автоматические тесты, проверяющие корректность математических вычислений.

Для запуска тестов выполните:

pytest tests/

text

---

## 🤖 CI/CD (Автоматизация)

Проект настроен на использование **GitHub Actions**.  
Файл конфигурации: `.github/workflows/check.yml`

### Как это работает:

1. При каждом `git push` (особенно при добавлении файлов в `uploads/`) запускается виртуальная машина.  
2. Устанавливаются зависимости и прогоняются тесты.  
3. Запускается скрипт антиплагиата.  
4. Сгенерированные отчёты (график и JSON) сохраняются в артефактах сборки (Artifacts), доступных преподавателю для скачивания.

---

## 📚 Автор
Дмитрий Демкин